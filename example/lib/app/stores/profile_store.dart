//TODO: After shared data feature is available in crystalline store create a ProfileStore
// that watches the shared user object managed by AuthStore and inside init() it will get the correct profile

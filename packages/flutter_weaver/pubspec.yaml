name: flutter_weaver
description: Best dependency injection library for Flutter, Rethought and redesigned specifically for flutter
version: 0.9.2
repository: https://github.com/easazade/weaver
issue_tracker: https://github.com/easazade/weaver/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  weaver: ^0.9.2

dev_dependencies:
  build_runner:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  weaver_builder: ^0.9.2
